#include <iostream>

int main()
{
  
  for (int number{0}; number <= 20 ; number += 2)  // Number adds up with 2 every execution beacuse it needs to show evem numbers
  {
    std::cout << "The number is: " << number << '\n';
  }
  

return 0;
}